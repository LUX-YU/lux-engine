add_subdirectory(domain)
add_subdirectory(process)
add_subdirectory(scene)
if(LUX_BUILD_PROFILE STREQUAL "EDITOR")
    add_subdirectory(editor)
elseif(LUX_BUILD_PROFILE STREQUAL "TOOLCHAIN")
    add_subdirectory(toolchain)
endif()
