//! lux-shader stage=vertex entry=main
#version 450
layout(location=0) noperspective out vec2 v_ndc;

vec2 V[3] = vec2[](vec2(-1.0,-1.0), vec2(3.0,-1.0), vec2(-1.0,3.0));

void main(){
    vec2 p = V[gl_VertexIndex];
    v_ndc = p;
    gl_Position = vec4(p,0.0,1.0);
}
