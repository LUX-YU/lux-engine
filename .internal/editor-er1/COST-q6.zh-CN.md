# ER-1 有限成本：q6

源码提交 `aa91ba553f2ab56c5670a9061d7c41b1336f0433`；正常 RelWithDebInfo，故障注入关闭。原始十个进程的JSON、日志和PPM在 `E:/lux-er1/q6/cost-pairs/`，产物身份见identity.json。每组交替先后顺序，五组全部保留，无剔除。

固定同一seed、1600×900窗口、1024×576目标、一个View、三个资源；100次独立预热、500个计量帧，8个读回验证Frame单独计数；两边逐帧均等到真实frame录制，再执行8ms节奏。所有进程显式关闭完成，最终像素checksum均11872603163728535614。

表内时间单位为秒，cycles和polls为计数；均为每进程500计量帧及注明等待范围的批量值，不是逐操作分位数。

| 指标 | 旧路径均值 | 新路径均值 | 新/旧 |
|---|---:|---:|---:|
| work_wall | 0.1050851 | 0.10432358 | 0.9928 |
| wait | 4.35184416 | 4.3514675 | 0.9999 |
| owner_cpu | 0.215625 | 0.175 | 0.8116 |
| process_cpu | 0.50625 | 0.4125 | 0.8148 |
| work_cycles | 333203326 | 328992482 | 0.9874 |
| wait_cycles | 307632017 | 334947593 | 1.0888 |
| work_polls | 500 | 500 | 1.0000 |
| wait_polls | 760598 | 502153.4 | 0.6602 |
| close_wall | 0.10854212 | 0.13493248 | 1.2431 |

第4组新路径关闭为0.2236169秒，其余新路径关闭为0.0982949—0.1195459秒；全部样本保留，不剔除该值，也不据五次运行推断稳定尾部分布。

work_wall是frame调用的墙钟时间，可能含内部阻塞和调度抢占。wait包含CPU背压/录制进度轮询、节奏等待及最终读回（含8个验证Frame）。work/wait cycles由QueryThreadCycleTime取得，不转换成CPU时间；线程/进程CPU秒来自Windows计数，短批次分辨率有限。polls只计驱动调用其poll函数的次数，不等同回复数或GPU工作量。

q5的五组原始样本完整保留，但新驱动逐帧等待录制、旧驱动只等CPU接纳，等待cycles因此不可直接归因于生产实现。仅提出并执行这一项窄假设：统一等待边界并补相同轮询计数。本轮没有凭计时修改Renderer调度，没有第二项无依据优化。

这组消费者驱动真实旧/新生产管线，但没有测量完整Application事件循环，也未覆盖交互布局变化、资源故障重试成本、连续resize尾部或owner分配/内存归因。不声称整体性能等价、正式GUI已验证或ER-1已通过。
