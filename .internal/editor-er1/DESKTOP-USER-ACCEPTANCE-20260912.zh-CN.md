# ER-1：用户人工桌面验收补记

日期：2026-09-12。承接源码候选 `4b9e6375be46c99b739939a29be3520828996792` 的测试收尾。
这是用户实际操作反馈，不是自动化工具生成的 GUI PASS。

用户原文：

> 我测试过了IME正常，鼠标捕获正常，可以正常使用中间和右键进行平移和旋转。

| 验收项 | 当前结论 | 依据 |
|---|---|---|
| 实际 IME 使用 | 人工验收通过 | 用户明确确认 IME 正常 |
| 实际鼠标捕获 | 人工验收通过 | 用户明确确认捕获正常 |
| 中键平移、右键旋转 | 人工验收通过 | 用户明确确认实际操作可用 |

此前“尚无人工确认”的状态被本次反馈补充；IME 的可用性问题已有用户复测通过证据。
不再把这两项一般使用功能列为等待 Computer Use 的阻塞，也不重复要求用户验证同一使用结果。
原 q7/q8 失败及旧日志保留原身份，不回写成历史 PASS。

本次反馈未逐项描述多显示器/DPI、失焦/隐藏/关闭时保持按键、具体码点/glyph 日志等细分步骤。
这些步骤不从“使用正常”自动推导为逐例实测通过。用户未提供被测 EXE 的精确 hash，
所以源码候选是当前交付上下文，不能据此给某一二进制伪造完整桌面矩阵。

另观察到新增运行目录 `desktop-diagnostic-20260912-170842`：

- process.log：16.485523 秒，frames=1744，descriptor=2/2，validation_errors=0。
- leases=0、views=0、accepted=0；exit.txt 为 0。
- 日志没有 selected_text 记录。它支持这次进程正常关闭，不独立证明输入内容或画面。

原源码和证据包保持不变；新增反馈单独交付。4b9 的 clean clone、136 项 CTest、19 个正常 GPU
变体、原/迁移 SDK 消费者和 20 个尾部成本进程的结果仍对应 4b9，不随本说明换源码标签。
本次仅记录验收，不改生产代码，不重复全量构建。

下一步是 ER-1 的旧正式入口切换及消费者收口：按 M01—M30 核对本阶段的实际引用、能力保留与
删除闭包后处理旧入口。历史 migration-status/capability 表需结合后续源码和证据核对，不能仅凭
旧 PARTIAL 标签或本次三项确认就批量删除。仍不进入 ER-2，不合并 main。
