# q8 正常候选有限成本

源码 `e6252e503670bf7037c1f275a0adae63276ae62f`，正常 RelWithDebInfo clean clone。沿用 q6 驱动，五组独立进程配对；100预热、500实际完成帧、8单列验证帧、1600×900隐藏窗口、1024×576目标、单View、3资源、8ms节奏。十张输出图像由驱动逐组核对；详见 cost-pairs/。

|指标|旧均值|新均值|变化|增加的配对数|
|---|---:|---:|---:|---:|
|work_wall|0.08161578|0.07928874|-2.851%|1/5|
|wait|4.29861182|4.30594696|+0.171%|5/5|
|work_cycles|245976237|244816985|-0.471%|2/5|
|wait_cycles|249890664|269758049|+7.950%|5/5|
|close_wall|0.09877154|0.09545166|-3.361%|2/5|

不将同图像等同于性能等价；wall包含内部阻塞与抢占，wait是显式等待，cycles不换算为GPU时间。不是G修正的隔离因果对照。旧q6/q7样本原样保留。

## P02 同输入旧候选/新候选配对

q7固定2d2650c5正常产物对本候选；每规模五组独立进程、交替顺序、100预热、500 owner循环、零View、空资源引用。每次公开snapshot身份/版本不变，checksum和真实关闭匹配；不代表READY资源或大型GPU成本。

64 Mesh：旧 [0.0030307, 0.0029877, 0.0029497, 0.0030358, 0.0029688] 秒，新 [0.0031557, 0.0031007, 0.0028525, 0.0028307, 0.0030832] 秒；均值 0.00299454 → 0.00300456 秒（+0.335%）。关联从源推导比较 2080 /cycle 改为 64 次map lookup/cycle；这不是CPU指令计数。索引key/value payload下界 1024 字节，不含bucket/node/allocator开销。

1024 Mesh：旧 [0.1102336, 0.1082303, 0.1095824, 0.1094155, 0.1104365] 秒，新 [0.0419317, 0.0419573, 0.0425305, 0.0436752, 0.0420033] 秒；均值 0.10957966 → 0.0424196 秒（-61.289%）。关联从源推导比较 524800 /cycle 改为 1024 次map lookup/cycle；这不是CPU指令计数。索引key/value payload下界 16384 字节，不含bucket/node/allocator开销。

## 冷字体工厂

每种五个独立正常进程；另一个默认UI会话A已建立，计量新UI冷工厂B的owning copy和atlas构建，不包含文件读取、Renderer上传或整个应用启动。不是稳态帧成本。

default: [0.0006359, 0.0008164, 0.000705, 0.0006392, 0.0006319] 秒；均值 0.00068568 秒。

font: [0.1497227, 0.146454, 0.1465722, 0.1463377, 0.1458532] 秒；均值 0.14698796 秒。

本轮完整owner分配账目、匹配resize/retry尾部分布仍未完成；不报告虚构p95。