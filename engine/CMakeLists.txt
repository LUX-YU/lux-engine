add_subdirectory(world)
add_subdirectory(simulation)
if(LUX_BUILD_PROFILE MATCHES "^(EDITOR|TOOLCHAIN)$")
    add_subdirectory(authoring)
endif()
if(LUX_BUILD_PROFILE STREQUAL "EDITOR")
    add_subdirectory(tools/editor/node_graph)
endif()
if(LUX_BUILD_PROFILE STREQUAL "TOOLCHAIN")
    add_subdirectory(toolchain)
    add_subdirectory(tools/toolchain/flowforge)
endif()
