#pragma once
// Generated module-local cooked section load projection. No registrar and no
// custom Component codec: each binding instantiates the generic typed thunk.
#include <lux/engine/ecs/ComponentLoadBinding.hpp>
#include <array>
{% if include_dir %}#include <{{ include_dir }}>{% else %}#include "{{ source_path }}"{% endif %}

namespace lux::ecs::generated
{
    [[nodiscard]] inline ComponentLoadContribution
    {{ projection_symbol }}ComponentLoadContribution() noexcept
    {
        const auto schemas = {{ projection_symbol }}ComponentSchemas();
        static const std::array bindings{
{% set schema_index = 0 %}{% set emitted = false %}{% for decl_index in marked_record_decls %}{% set decl = decl_from_index(decl_index) %}{% if annotation_has(decl, "component") %}{% if annotation_get_or(decl, "world_section", "omit") == "load" %}{% if emitted %},
{% endif %}            bindComponentLoad<::{{ decl.fq_name }}>(schemas[{{ schema_index }}u]){% set emitted = true %}{% endif %}{% set schema_index = schema_index + 1 %}{% endif %}{% endfor %}
        };
        return {{ "{" }}{}, bindings{{ "}" }};
    }
} // namespace lux::ecs::generated
