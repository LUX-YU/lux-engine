#pragma once

// HDRColorTag removed — cross-feature GPU resource sharing now uses
// builder.referenceTexture("name") forward references instead of publish/resolve.

namespace lux::render
{
} // namespace lux::render
