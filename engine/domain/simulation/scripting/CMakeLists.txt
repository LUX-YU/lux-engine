add_subdirectory(core)
add_subdirectory(cpp_static)
if(TARGET script_native)
    add_subdirectory(native)
endif()
if(TARGET script_lua AND NOT ANDROID)
    add_subdirectory(lua)
    add_subdirectory(native_lua_tasks)
endif()
