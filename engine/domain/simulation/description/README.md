# Simulation Description

`lux::engine::simulation::description` provides immutable, canonical L1 data
describing synchronous simulation providers and their opaque configuration.
It does not serialize systems, task graphs, runtime ECS state, functions, or
loaded module objects.
